import {
  serverTimestamp,
  firestoreDataBase,
  signin,
  signout,
  ruleOut,
  launchEmailVerification,
  launchSigninStateObserver,
  getUserCredentials,
} from './firebaseConfig.js';

export {
  serverTimestamp,
  firestoreDataBase,
  signin,
  signout,
  ruleOut,
  launchEmailVerification,
  launchSigninStateObserver,
  getUserCredentials,
};
